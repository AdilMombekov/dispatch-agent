# Environment / secrets
.env
.env.local

# Runtime config (contains token — use config.example.json for reference)
config.json
config.json.*

# Runtime state (live data, machine-specific — never commit)
agent_state.json
chats.json
backup_index.json
claude_task.json
claude_runs.jsonl
tasks.db
tasks.db-wal
tasks.db-shm
inbox/
claude_accounts/
trash/

# Logs
*.log

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# PyInstaller build artifacts (keep build/build.py)
build/DispatchAgent/
dist/
*.spec
# …but DO track our spec — it carries hiddenimports needed for a working build.
!DispatchAgent.spec

# Node (Electron Control Center)
node_modules/

# Junk / build artifacts
_npm_start.*
*.zip
build_log.txt

# Claude Code local settings
.claude/

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store
